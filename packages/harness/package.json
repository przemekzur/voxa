{
  "name": "voxa-harness",
  "private": true,
  "version": "1.1.1",
  "type": "module",
  "description": "Voxa connector harness — hosts connectors (voice tools), the forge build-agent, and the local Markdown brain, speaking the voice-tool contract (GET/POST /api/voice/tools).",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node server.mjs"
  },
  "dependencies": {
    "express": "^5"
  }
}
